# Only for a separately authorized source export, not for tests or the live API.
# export.mjs does not load .env files. Supply this explicitly in its environment.
# Use a PostgreSQL credential with SELECT access to all required public tables
# and sequences and permission to read every row despite RLS. Leave empty until
# the source and write-freeze procedure have been reviewed.
D1_SOURCE_DATABASE_URL=
